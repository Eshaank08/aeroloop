"""Simulator v2: a rate controlled quadrotor inspection task.

v1 in `sim/` is a point mass commanded in acceleration. v2 keeps the same nacelle,
the same seeded winds and the same verdict shape, and replaces the vehicle with a
rate controlled quadrotor plus a camera gate on coverage. See docs/SIM2_SPEC.md.
"""
